nodes:
  [
    {
      #node1
      url: "38.242.233.94",
      username: "root",
      password: "AFlGaqdn2oH38Meo0ze",
      constants:
        {
          FILE_NAME: "node1", 
        },
    },
   {
     #node2
     url: "185.190.142.207",
     username: "root",
     password: "H3xW778KsCv2AoDJ",
     constants:
       {
         FILE_NAME: "node2",
       },
   },
  ]

commands: [
#
# Удаляем старую директорию remote-ssh (если она есть) и клонируем на ноду репо со всеми нашими bash-скриптами.
# Данные bash-скрипты содержат пользовательские команды для работы с нодами.
'rm -rf $HOME/remote-ssh && git clone -q https://github.com/UNITED-CRYPTO/remoteSSH-Scripts.git remote-ssh',
#
# Устанавливаем execute-разрешение для bash-скрипта.
'chmod +x $HOME/remote-ssh/create_file_git.sh',
#
# Загружаем натройки переменных окружения (как в интерактивной сессии), за тем передаем пользовательские переменные в окружение ноды и запускаем bash-скрипт, склонированный из репо.
# Важно это делать в одной строке (одной командой) из-за того, что отдельные команды у нас пока выполняются в контексте разных сессий (Подробнее см.: https://github.com/Serge-GHSHCH/UCrypto/issues/10).
'source /etc/profile && source ~/.bash_profile && export FILE_NAME="<FILE_NAME>" && $HOME/remote-ssh/create_file_git.sh',
#
]